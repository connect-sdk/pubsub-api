# prettier-ignore
---
version: v1

name: buf.build/connect-sdk/pubsub-api

deps:
  - buf.build/googleapis/googleapis
  - buf.build/envoyproxy/protoc-gen-validate

lint:
  use:
    - DEFAULT
  allow_comment_ignores: true
